package ch10;

public class Solution07 {

	public static void main(String[] args) {
		/*
		 * When we have 1GB memory available: 1 GB = 2^30. Since this memory is
		 * enough to store 4 billion bits We will solve the problen using
		 * bitmaps
		 * 
		 * When we just have 10 MB using bitmap for each number wont be
		 * possible, However we can have counts of elements that fall under
		 * certain buckets. And later if the bucket count does not match the
		 * expected count, we further explore the elements from that bucket
		 * using bitmaps
		 */

	}

}
