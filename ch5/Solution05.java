package ch5;

public class Solution05 {
	/*
	 * It basically tells if a number is a perfect power of two.
	 * 
	 * If a number is a power of two it will have only one 1 in its 
	 * binary representation which means one less that number would 
	 * have all 1s which are at a position othr than the 1 in first number
	 *  which means &ing them would give a zero
	 * 
	 */
}
